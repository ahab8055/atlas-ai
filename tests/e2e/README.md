# End-to-end tests (Playwright)

Reserved for cross-cutting UI / desktop flows.

Per Technology Stack: **Playwright** for e2e. Scaffold and CI wiring come in a later foundation / quality pass.

Until then:

```bash
pnpm test        # unit tests only
pnpm test:rust   # Rust unit tests
```
